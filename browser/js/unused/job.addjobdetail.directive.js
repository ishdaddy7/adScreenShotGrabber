/*app.directive('addJobDetail', function($compile){
  return {
    restrict: 'A',
    link: function(scope, element, attributes){
      element.bind('click', function(){
        element.parent().append($compile('<job-detail detailCount=' + scope.detailCount + '></job-detail>')(scope));
      });
    }
  }
});
*/
